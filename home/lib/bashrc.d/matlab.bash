# made matlab work before but not needed anymore
#export MATLAB_JAVA=/usr/lib/jvm/java-6-sun/jre

# makes matlab work in dwm/monad, but breaks matlab in rio
# also breaks newer matlab
#export AWT_TOOLKIT=MToolkit

export MATLABPATH=$HOME/csdirs/src/matlab
